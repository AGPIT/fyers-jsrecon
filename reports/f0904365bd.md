# https://trade.fyers.in/static/js/exit-widget/assets/js/2.5/eo.min.js
SECRET|other|auth_token and token_id are passed as request variables (Authorization header + query param), not hardcoded values
ENDPOINT|POST|/positions?token_id={token_id}
ENDPOINT|POST|https://data.fyers.in/dev-fyers/mobileapi/user-settings
HOST|data.fyers.in
NOTE|Code is obfuscated. The positions endpoint base host comes from globalConstants (not present in this file); only the relative path /positions?token_id= is hardcoded.
NOTE|A dev/staging user-settings endpoint lives under /dev-fyers/ on data.fyers.in, suggesting dev-tier deployment of the exit-widget.
