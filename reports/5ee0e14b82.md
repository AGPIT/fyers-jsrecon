# https://trade.fyers.in/api-login/static/js/validations.js
NOTE|Validation regex patterns for Fyers Client ID (2 letters + 3-5 digits), PAN (5A4N1A) and DOB (DD-MM-YYYY)
NOTE|Bug in addMethod("pattern"): "return this.optional(element) || regexp.test(value);" sits outside the else branch (lines 6-8), so it runs unconditionally
NOTE|Debugging console.log statements left in production code (lines 12, 17) in panValidate and dobValidate methods
NONE_ANALYSABLE
